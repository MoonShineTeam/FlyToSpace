<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainMenuText">
        <item name="android:textSize">26sp</item>
        <item name="android:fontFamily">@font/obelix_pro</item>
    </style>

    <style name="RatingBar" parent="@style/Widget.AppCompat.RatingBar">
        <item name="android:numStars">3</item>
        <item name="android:isIndicator">true</item>
        <item name="android:progressBackgroundTint" tools:targetApi="lollipop">@color/yellow50</item>
        <item name="android:progressTint" tools:targetApi="lollipop">@color/yellow</item>
        <item name="android:secondaryProgressTint" tools:targetApi="lollipop">@color/empty</item>
        <item name="android:scaleX">0.7</item>
        <item name="android:scaleY">0.7</item>
        <item name="android:minHeight">10dp</item>
    </style>
</resources>